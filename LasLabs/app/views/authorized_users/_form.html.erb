<%= form_for(@authorized_user) do |f| %>
  <% if @authorized_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@authorized_user.errors.count, "error") %> prohibited this authorized_user from being saved:</h2>

      <ul>
      <% @authorized_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :company %><br>
    <%= f.number_field :company %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
